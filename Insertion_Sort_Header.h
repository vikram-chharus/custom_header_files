#include "Swap.h"
void insertionSort(int array[],int base_index,int last_index)
{
	int index=base_index;
	for(int i=base_index;i<=last_index;i++)
	{
		for(int j=i+1;j<=last_index;j++)
		{
			if(array[i]<)
		}
	}
}
